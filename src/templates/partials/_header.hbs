<nav class="navbar">
  <div class="navbar-header">
    <button class="collapsed navbar-toggle" data-toggle="collapse" data-target="#header-nav">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a href="/" class="navbar-brand">
      <span class="logotype"></span>
    </a>
  </div>
  <div class="collapse navbar-collapse" id="header-nav">
    <ul class="nav navbar-nav navbar-left">
      {{#unless user}}
        <li><a href="/login">Log In</a></li>
        <li><a href="/signup">Sign Up</a></li>
      {{/unless}}
    </ul>

    {{#if user}}
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown" id="current-user">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            {{#if user.photoURL}}
            <span class="img-circle" style="background-image:url('{{ user.photoURL }}')"></span>
            {{/if}}
            {{#if user.displayName}}
            {{ user.displayName }}
            {{else}}
            {{ user.email }}
            {{/if}}
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <li><a href="/profile">Profile</a></li>
            <li><a href="/profile/edit">Edit profile</a></li>
            <li class="divider"></li>
            <li><a href="/logout">Logout</a></li>
          </ul>
        </li>
      </ul>
    {{/if}}
  </div>
</nav>
